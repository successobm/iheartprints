/** Re-declared for `@/domain` consumers without importing value arrays from types. */
export { CONVERSATION_PHASES, PROJECT_STATUSES } from "@/lib/domain/types";

export const ARTWORK_KINDS = ["concept", "revision", "final"] as const;
